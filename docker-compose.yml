version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - redis

  frontend:
    build: ./frontend/my-app
    ports:
      - "3000:3000"

  redis:
    image: redis:latest
    ports:
      - "6379:6379"